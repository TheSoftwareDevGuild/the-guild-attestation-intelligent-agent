[project]
name = "attestation-frontend"
version = "0.1.0"
description = "Frontend UI for Attestation Intelligent Agent"
requires-python = ">=3.12"

# Minimal dependencies for frontend
dependencies = [
    "streamlit>=1.50.0",
    "requests>=2.32.5",
    "pydantic>=2.12.0",
    "pydantic-settings>=2.11.0",
]