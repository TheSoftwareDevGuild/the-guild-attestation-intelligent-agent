[project]
name = "attestation-backend"
version = "0.1.0"
description = "Backend API for Attestation Intelligent Agent"
requires-python = ">=3.12"

# Minimal dependencies for backend
dependencies = [
    "fastapi>=0.118.0",
    "uvicorn>=0.37.0",
    "gunicorn>=21.2.0",
    "pydantic>=2.12.0",
    "pydantic-settings>=2.11.0",
    "qdrant-client>=1.15.1",
    "openai>=1.109.1",
    "requests>=2.32.5",
    "pyyaml>=6.0.2",
    "instructor>=1.11.3",
    "langsmith>=0.4.32",
    "fastembed>=0.7.3",
]
